<%= form_with model: @income, local: true do |f| %>
 
  <% if @income.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@income.errors.count, "error") %> prohibited
        this income from being saved:
      </h2>
      <ul>
        <% @income.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
    <p>
    <%= f.label :tytuł %><br>
    <%= f.text_field :title %>
  <br>
 
    <%= f.label :opis %><br>
    <%= f.text_area :text %>
  <br>
  
    <%= f.label :kwota %><br>
    <%= f.number_field :amount, min: 0.0, step: 0.01 %>
  <br>
  
	<%= f.label :kategoria %><br>
	<%= f.select :category_id, @categories, { include_blank: false } %>
  <br>
  
    <%= f.submit :Zapisz %>
  </p>
<% end %>