<%= form_with model: @outgoing, local: true do |f| %>
 
  <% if @outgoing.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@outgoing.errors.count, "error") %> prohibited
        this outgoing from being saved:
      </h2>
      <ul>
        <% @outgoing.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
    <p>
    <%= f.label :tytuł %><br>
    <%= f.text_field :title %>
  <br>
 
    <%= f.label :opis %><br>
    <%= f.text_area :text %>
  <br>
  
    <%= f.label :kwota %><br>
    <%= f.number_field :amount, max: 0.0, step: 0.01 %>
  <br>
  
	<%= f.label :kategoria %><br>
	<%= f.select :category_id, @categories, { include_blank: false } %>
  <br>
 
    <%= f.submit :Zapisz %>
  </p>
<% end %>